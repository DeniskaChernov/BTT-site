# Content publishing

Тексты — `messages/{locale}.json`. Метаданные — `src/content/articles/meta.json`, `contentKind` в `src/data/articles.ts`.

MDX: добавить `src/content/articles/{slug}.mdx` и slug в `MDX_SLUGS` (`article-content.ts`).
