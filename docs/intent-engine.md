# Intent Engine (Bententrade)

Единый слой рекомендаций: профиль `btt_intent_v1`, граф `commerce-graph`, ранжирование `rankProducts` / `rankArticles`.

Journey выводится из поведения (`infer-journey.ts`), не из кнопок в UI.

События: `src/lib/intent/events.ts`. CRM snapshot: `intentProfileToLeadSnapshot`.
